
spring:

  devtools:
    restart:
      enabled: false

  datasource:

    url: jdbc:postgresql://localhost:5432/rateguard
    username: YOUR_USERNAME
    password: YOUR_PASSWORD

  jpa:

    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379